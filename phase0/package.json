{
  "name": "skill-asset-phase0",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Phase 0 testnet spike: wallet-attested Skill registration and declared Derivative ancestry on Story Aeneid.",
  "scripts": {
    "check": "node --import tsx src/index.ts check",
    "demo": "node --import tsx src/index.ts demo",
    "recover-stale-lock": "node --import tsx src/index.ts recover-stale-lock",
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@story-protocol/core-sdk": "^1.4.4",
    "dotenv": "^16.4.5",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}

{
  "name": "pi-wielder-spike",
  "version": "0.1.0",
  "private": true,
  "description": "Pi-Wielder spike: one wallet, two asset classes. A coding harness pays per-call x402 for model inference AND a hosted Skill behind a mock collar, with a unified attributed session ledger. Testnet-only (Base Sepolia); fully offline in mock mode.",
  "type": "module",
  "scripts": {
    "e2e": "MOCK_FACILITATOR=1 MOCK_LLM=1 node e2e.mjs",
    "collar": "node src/collar.mjs",
    "gateway": "node src/gateway.mjs",
    "proxy": "node src/proxy.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "@hono/node-server": "^2.0.8",
    "hono": "^4.12.29",
    "viem": "^2.55.0"
  }
}
